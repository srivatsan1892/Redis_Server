{
  "name": "redis_server",
  "version": "1.0.0",
  "description": "Redis is a in memory database used to store key-value pair \r Redis- Server by default runs on 6379 port number\r Clients can be - Node Server, Python Client , Rust Client , IOT and so on \r The communication happens with TCP protocol \r Initially a TCP server is built which listens to 6379 port",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "redis-parser": "^3.0.0"
  }
}
